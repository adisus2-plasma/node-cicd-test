version: 2.1
orbs:
  azure-cli: circleci/azure-cli@1.2.0

jobs:
  example-job:
    docker:
      # replace with your preferred image
      - image: cimg/base:stable
    steps:
      - run:
         command: | 
                 ssh $SSH_PUB
                 -y

workflows:
  example-workflow:
    jobs:
      - example-job
